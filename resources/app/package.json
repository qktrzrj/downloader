{
  "name": "downloader",
  "productName": "downloader",
  "description": "the fasted downloader",
  "keywords": [
    "downloader"
  ],
  "main": "./main.js",
  "version": "1.0.0",
  "author": "yunyan",
  "scripts": {
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "start": "electron .",
    "package": "electron-packager . downloader --platform=win32 --out Downloader --arch=x64 --electron-version=7.0.0 --overwrite --ignore=node_modules --ignore=.gitignore"
  },
  "dependencies": {
    "electron-log": "^3.0.8"
  },
  "devDependencies": {
    "electron": "7.0.0",
    "electron-winstaller": "^4.0.0"
  },
  "build": {
    "appId": "online.yinyan.www",
    "win": {}
  }
}
